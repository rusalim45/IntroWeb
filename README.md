# Task Manager

Flask + SQLite + HTML/CSS/JS pet project.

## Setup

```bash
python -m venv venv
source venv/bin/activate   # Mac/Linux
venv\Scripts\activate      # Windows

pip install -r requirements.txt
```

## Run

```bash
python run.py
```

Open http://127.0.0.1:5000
